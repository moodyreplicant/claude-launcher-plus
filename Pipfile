[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
jsonschema = ">=4.26.0"  # Required for provider config validation

[dev-packages]
pipenv = ">=2026.6.2"
pre-commit = ">=4.6.0"
bandit = ">=1.9.4"
mypy = ">=2.1.0"
flake8 = ">=7.3.0"
black = ">=26.5.1"
isort = ">=8.0.1"
pytest = ">=9.0.3"
pytest-cov = ">=7.1.0"
pip-audit = ">=2.9.0"

[requires]
python_version = "3.10"
